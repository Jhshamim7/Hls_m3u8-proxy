{
  "name": "cloudflare-proxy-worker",
  "version": "1.0.0",
  "type": "module",
  "description": "A Cloudflare Worker proxy deployable from GitHub",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0 --port 3000",
    "build": "echo 'No build step required for Cloudflare Workers'",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240314.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}
